/** Number of random bytes used to generate a raw SDK token. */
export const TOKEN_BYTES = 32;

/** Fixed prefix prepended to every raw SDK token. */
export const TOKEN_PREFIX = 'pluma_sdk_';

/** Number of characters stored as the displayable token prefix (first N chars of the raw token). */
export const TOKEN_PREFIX_LENGTH = 12;
