export const PUBLIC_PATHS = ['/login', '/register'];

export const ROUTES = {
  projects:     '/projects',
  environments: '/environments',
  flags:        '/flags',
  audit:        '/audit',
  settings:     '/settings',
} as const;
