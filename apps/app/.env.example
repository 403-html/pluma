# Example environment variables

# API URL for server-side calls (middleware, server components)
API_URL=http://localhost:4000

# API URL for client-side calls (browser)
NEXT_PUBLIC_API_URL=http://localhost:4000
