# Example environment variables

# API base URL — used by both middleware (server-side) and browser (client-side)
NEXT_PUBLIC_API_URL=http://localhost:2137

# To raise the 4096 MB dev heap cap, pass NODE_OPTIONS before the pnpm command:
#   NODE_OPTIONS=--max-old-space-size=8192 pnpm dev
# Setting NODE_OPTIONS here has no effect — .env is loaded inside the running process,
# too late to influence V8 heap configuration.
