# Example environment variables

# API base URL — used by both middleware (server-side) and browser (client-side)
NEXT_PUBLIC_API_URL=http://localhost:2137

# Cap Node.js heap to prevent unbounded growth in dev; raise if OOM still occurs.
# If NODE_OPTIONS is already set in your shell, the dev script's default is skipped —
# include --max-old-space-size=N in your own NODE_OPTIONS to keep the cap.
NODE_OPTIONS=--max-old-space-size=4096
