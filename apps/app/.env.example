# Example environment variables

# API base URL — used by middleware and server-side code to proxy requests to the API server.
# Never exposed to the browser (all usages are server-side only).
NEXT_PUBLIC_API_URL=http://localhost:2137
