# ── Build output ─────────────────────────────────────────────────────────────
**/dist
**/.next
**/.turbo
**/coverage

# ── Dependencies (reinstalled inside the image) ───────────────────────────────
**/node_modules

# ── Environment files (never bake secrets into images) ────────────────────────
.env
.env.*
!.env.example

# ── Version control ───────────────────────────────────────────────────────────
.git
.gitignore
.gitattributes

# ── CI / IDE / tooling ────────────────────────────────────────────────────────
.github
.vscode
.idea

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log

# ── Lock files (pnpm-lock.yaml is intentionally kept) ────────────────────────
*.lock
!pnpm-lock.yaml

# ── Compose / Dockerfile artefacts ───────────────────────────────────────────
docker-compose*.yml
**/Dockerfile*
