# Docker Compose Environment Variables
# Copy this file to .env and customize for your environment

# PostgreSQL Configuration
POSTGRES_USER=pluma
POSTGRES_PASSWORD=pluma
POSTGRES_DB=pluma

# Next.js Public API URL (embedded at build time)
# Development: use localhost since browser runs on host machine
NEXT_PUBLIC_API_URL=http://localhost:4000

# For production, use strong credentials:
# 1. Generate a URL-safe password and save to password manager immediately:
#    openssl rand -hex 32
# 2. Then set in .env:
# POSTGRES_USER=pluma_prod
# POSTGRES_PASSWORD=<paste-generated-password-here>
# POSTGRES_DB=pluma_production
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
