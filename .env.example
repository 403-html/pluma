# Pluma — runtime environment variables
#
# Copy this file to .env and fill in the values before running docker compose.
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to source control.

# ── Database ─────────────────────────────────────────────────────────────────
# Connection string for the PostgreSQL instance.
# When using the bundled docker-compose.yml the hostname is "postgres" (the
# service name on the internal pluma network).
DATABASE_URL=postgresql://pluma:pluma@postgres:5432/pluma?schema=public

# ── API ──────────────────────────────────────────────────────────────────────
# TCP port the Fastify server listens on inside the container.
PORT=2137

# Secret used to sign session cookies. Must be a long, random string.
# Generate one with: openssl rand -hex 32
SESSION_SECRET=REPLACE_WITH_A_STRONG_RANDOM_SECRET

# Runtime environment — keep this as "production" for self-hosted deployments.
NODE_ENV=production

# ── App ──────────────────────────────────────────────────────────────────────
# Base URL of the API as seen by the Next.js server (server-side rewrites).
# When using docker-compose the hostname is "api" (the service name).
NEXT_PUBLIC_API_URL=http://api:2137
